<UserControl x:Class="ccc.Controls.Buttons.SidebarToggle"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="32" d:DesignWidth="32">
    <Button Style="{StaticResource InvisibleBtn}" Command="{Binding ToggleCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
            ToolTip="Toggle Sidebar">
        <Grid>
            <!-- Hamburger Icon (Open) -->
            <TextBlock Text="☰" FontSize="16" VerticalAlignment="Center" HorizontalAlignment="Center"
                       Visibility="{Binding IsOpen, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>
            
            <!-- Close Icon (X) -->
            <TextBlock Text="✕" FontSize="14" VerticalAlignment="Center" HorizontalAlignment="Center"
                       Visibility="{Binding IsOpen, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </Grid>
    </Button>
</UserControl>
