<UserControl x:Class="ccc.Controls.Cards.FolderCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             mc:Ignorable="d" d:DesignWidth="320" d:DesignHeight="220">
             
    <!-- Similar to PlaylistCard Base -->
    <Border Style="{StaticResource CardBaseStyle}" Padding="0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> 
                <RowDefinition Height="*"/>    
                <RowDefinition Height="Auto"/> 
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Margin="12,12,12,8" 
                    Background="#F1F5F9" BorderBrush="{Binding FolderBrush, RelativeSource={RelativeSource AncestorType=UserControl}}" BorderThickness="1" CornerRadius="4"
                    Padding="8,4">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Text="{Binding FolderTitle, RelativeSource={RelativeSource AncestorType=UserControl}}" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" 
                               TextTrimming="CharacterEllipsis"/>
                </Grid>
            </Border>

            <!-- Folder Icon Thumbnail -->
            <Grid Grid.Row="1" Margin="12,0,12,0" Height="140">
                <Border CornerRadius="8" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1">
                    <Viewbox Margin="24">
                        <!-- Simple Folder Path -->
                        <Path Data="M20,6h-8l-2-2H4C2.9,4,2,4.9,2,6v12c0,1.1,0.9,2,2,2h16c1.1,0,2-0.9,2-2V8C22,6.9,21.1,6,20,6z" 
                              Fill="{Binding FolderBrush, RelativeSource={RelativeSource AncestorType=UserControl}}" Stretch="Uniform"/>
                    </Viewbox>
                </Border>
            </Grid>

            <!-- Footer -->
            <StackPanel Grid.Row="2" Margin="12,8,12,12">
                <TextBlock Text="{Binding VideoCountText, RelativeSource={RelativeSource AncestorType=UserControl}}" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
            </StackPanel>
            
        </Grid>
    </Border>
</UserControl>
