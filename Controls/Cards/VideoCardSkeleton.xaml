<UserControl x:Class="ccc.Controls.Cards.VideoCardSkeleton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="260" d:DesignWidth="300">
    <Border Background="White" CornerRadius="12" Margin="0,0,0,24" BorderThickness="0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="160"/> <!-- Thumbnail -->
                <RowDefinition Height="Auto"/> <!-- Text -->
            </Grid.RowDefinitions>

            <!-- Thumbnail Placeholder -->
            <Border Grid.Row="0" CornerRadius="12" Background="#E2E8F0" Margin="4"/>

            <!-- Text Placeholders -->
            <StackPanel Grid.Row="1" Margin="8,8,8,4">
                <!-- Avatar + Title Row -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="32"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Avatar circle -->
                    <Border Grid.Column="0" Width="32" Height="32" CornerRadius="16" Background="#E2E8F0" VerticalAlignment="Top"/>

                    <!-- Title Bars -->
                    <StackPanel Grid.Column="1" Margin="8,0,0,0">
                        <Border Height="16" Background="#E2E8F0" CornerRadius="4" Margin="0,0,20,4"/>
                        <Border Height="16" Background="#E2E8F0" CornerRadius="4" Width="140" HorizontalAlignment="Left"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
