<UserControl x:Class="ccc.Controls.Cards.PlaylistCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="220" d:DesignWidth="300"
             Cursor="Hand">
    <UserControl.Resources>
        <Style x:Key="CardHover" TargetType="Border">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1AFFFFFF"/>
                    <Setter Property="BorderBrush" Value="#33FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Border Style="{StaticResource CardHover}" BorderThickness="1" CornerRadius="8" Padding="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Thumbnail (16:9) -->
                <RowDefinition Height="*"/>    <!-- Content -->
            </Grid.RowDefinitions>

            <!-- Thumbnail Container -->
            <Border Grid.Row="0" CornerRadius="6" ClipToBounds="True" Background="#111">
                <Grid>
                    <!-- Aspect Ratio Hack: Width=Content, Height=Auto -->
                    <!-- We'll just fix height for now or use a ViewBox -->
                    <Image Source="{Binding ThumbnailUrl}" Stretch="UniformToFill" Height="160"/>
                    
                    <!-- Fallback -->
                    <!-- In real impl, use a DataTrigger to show fallback if Source is null -->
                    
                    <!-- Video Count Badge -->
                    <Border Background="#CC000000" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="6" CornerRadius="4" Padding="6,2">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE768;" FontFamily="Segoe MDL2 Assets" Foreground="White" FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0"/>
                            <TextBlock Text="{Binding VideoCountString}" Foreground="White" FontSize="11" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <!-- Metadata -->
            <StackPanel Grid.Row="1" Margin="4,8,4,0">
                <TextBlock Text="{Binding Name}" Foreground="White" FontWeight="Bold" FontSize="14" TextTrimming="CharacterEllipsis"/>
                <TextBlock Text="{Binding Description}" Foreground="#94a3b8" FontSize="12" Margin="0,2,0,0" TextTrimming="CharacterEllipsis" MaxHeight="34"/>
                
                <!-- Footer -->
                <TextBlock Text="{Binding CreatedAtString}" Foreground="#64748b" FontSize="11" Margin="0,6,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
