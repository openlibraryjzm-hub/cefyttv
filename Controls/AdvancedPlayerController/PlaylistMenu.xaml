<UserControl x:Class="ccc.Controls.AdvancedPlayerController.PlaylistMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="102" d:DesignWidth="340">
    <UserControl.Resources>
        <Style x:Key="IconBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#aaa"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="24"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Background" Value="#33FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="#CC000000" BorderBrush="#333" BorderThickness="1" CornerRadius="12" Width="340" Height="102">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/> <!-- Title Area -->
                <RowDefinition Height="Auto"/> <!-- Bottom Bar -->
            </Grid.RowDefinitions>

            <!-- Title Area -->
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,-10,0,0">
                <TextBlock Text="Current Playlist" Foreground="White" FontWeight="Bold" FontSize="15" HorizontalAlignment="Center"/>
                <Border Background="#FF0000" CornerRadius="4" Padding="6,1" HorizontalAlignment="Center" Margin="0,4,0,0" Visibility="Visible">
                    <TextBlock Text="Folder Name" Foreground="White" FontSize="10" FontWeight="SemiBold"/>
                </Border>
            </StackPanel>

            <!-- Priority Pin (Top Right) -->
            <Border HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,8,8,0" Width="20" Height="20" Background="#fbbf24" CornerRadius="10" Visibility="Visible">
                <!-- Pin Icon Placeholder -->
                <TextBlock Text="P" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10" FontWeight="Bold" Foreground="Black"/>
            </Border>

            <!-- Bottom Bar -->
            <Grid Grid.Row="1" Height="32" Margin="12,0,12,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/> <!-- Metadata -->
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/> <!-- Controls -->
                </Grid.ColumnDefinitions>

                <!-- Metadata -->
                <TextBlock Text="Author | 1.2M Views | 2024" Foreground="#94a3b8" VerticalAlignment="Center" FontSize="11" FontWeight="Medium"/>

                <!-- Controls -->
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Content="&#xE760;" FontFamily="Segoe MDL2 Assets" Style="{StaticResource IconBtn}" ToolTip="Previous Playlist" Margin="0,0,4,0"/> <!-- ChevronLeft -->
                    <Button Content="&#xE80A;" FontFamily="Segoe MDL2 Assets" Style="{StaticResource IconBtn}" ToolTip="Grid" Margin="0,0,4,0"/> <!-- ViewAll -->
                    <Button Content="&#xE761;" FontFamily="Segoe MDL2 Assets" Style="{StaticResource IconBtn}" ToolTip="Next Playlist"/> <!-- ChevronRight -->
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</UserControl>
