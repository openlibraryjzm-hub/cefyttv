<UserControl x:Class="ccc.Controls.AdvancedPlayerController.VideoMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="102" d:DesignWidth="340">
    <UserControl.Resources>
        <Style x:Key="IconBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#aaa"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="24"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Margin" Value="1,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="12" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Background" Value="#33FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="ActionBtn" TargetType="Button" BasedOn="{StaticResource IconBtn}">
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#334155"/>
        </Style>
    </UserControl.Resources>

    <Border Background="#CC000000" BorderBrush="#333" BorderThickness="1" CornerRadius="12" Width="340" Height="102">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/> <!-- Title Area -->
                <RowDefinition Height="Auto"/> <!-- Bottom Bar -->
            </Grid.RowDefinitions>

            <!-- Title Area -->
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,-10,0,0">
                <TextBlock Text="Video Title Goes Here" Foreground="White" FontWeight="Bold" FontSize="14" TextTrimming="CharacterEllipsis" MaxWidth="320" HorizontalAlignment="Center"/>
                <TextBlock Text="Channel Name • 250K Views" Foreground="#94a3b8" FontSize="11" HorizontalAlignment="Center" Margin="0,2,0,0"/>
            </StackPanel>

            <!-- Bottom Bar Controls -->
            <Grid Grid.Row="1" Height="40" Margin="10,0,10,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/> <!-- Nav Cluster -->
                    <ColumnDefinition Width="*"/>    <!-- Spacer -->
                    <ColumnDefinition Width="Auto"/> <!-- Action Cluster -->
                    <ColumnDefinition Width="Auto"/> <!-- Menu -->
                </Grid.ColumnDefinitions>

                <!-- 1. Nav Cluster -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="&#xE760;" FontFamily="Segoe MDL2 Assets" Style="{StaticResource IconBtn}" ToolTip="Prev Video"/>
                    <Button Content="&#xE80A;" FontFamily="Segoe MDL2 Assets" Style="{StaticResource IconBtn}" ToolTip="Grid View"/>
                    <Button Content="&#xE761;" FontFamily="Segoe MDL2 Assets" Style="{StaticResource IconBtn}" ToolTip="Next Video"/>
                    <Button Content="&#xE768;" FontFamily="Segoe MDL2 Assets" Style="{StaticResource IconBtn}" Margin="4,0,0,0" ToolTip="Play (Standard)"/>
                </StackPanel>
                
                <!-- 2. Action Cluster -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="&#xE734;" FontFamily="Segoe MDL2 Assets" Style="{StaticResource ActionBtn}" ToolTip="Star Folder" Margin="0,0,2,0"/>
                    <Button Content="&#xE8B1;" FontFamily="Segoe MDL2 Assets" Style="{StaticResource ActionBtn}" ToolTip="Shuffle" Margin="0,0,2,0"/>
                    <Button Content="&#xE718;" FontFamily="Segoe MDL2 Assets" Style="{StaticResource ActionBtn}" ToolTip="Pin" Margin="0,0,2,0"/>
                    <Button Content="&#xE8E1;" FontFamily="Segoe MDL2 Assets" Style="{StaticResource ActionBtn}" ToolTip="Like" Margin="0,0,2,0"/>
                    <Button Content="&#xE946;" FontFamily="Segoe MDL2 Assets" Style="{StaticResource ActionBtn}" ToolTip="Info"/>
                </StackPanel>
                
                 <!-- 3. Menu -->
                <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,0,0">
                    <Button Content="&#xE712;" FontFamily="Segoe MDL2 Assets" Style="{StaticResource IconBtn}" ToolTip="More Options"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</UserControl>
