<UserControl x:Class="ccc.Controls.AdvancedPlayerController.PlayerOrb"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="160" d:DesignWidth="160">
    <Grid Width="154" Height="154">
        <!-- Visualizer Rings (Placeholder) -->
        <Ellipse Width="154" Height="154" Stroke="#0ea5e9" StrokeThickness="2" Opacity="0.5"/>
        <Ellipse Width="148" Height="148" Stroke="#0284c7" StrokeThickness="1" Opacity="0.3"/>
        
        <!-- Main Image Circle -->
        <Ellipse Width="144" Height="144">
            <Ellipse.Fill>
                <ImageBrush ImageSource="https://picsum.photos/200" Stretch="UniformToFill"/>
            </Ellipse.Fill>
        </Ellipse>
        
        <!-- Spill Effect Placeholder (Invisible by default) -->
        
        <!-- Hover Overlay (Buttons Placeholder) -->
        <Grid x:Name="HoverOverlay" Opacity="0">
             <Grid.Style>
                <Style TargetType="Grid">
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                             <Setter Property="Opacity" Value="1"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
             </Grid.Style>
             
             <!-- Upload Icon -->
             <Border Background="#88000000" VerticalAlignment="Top" HorizontalAlignment="Center" Height="40" Width="100" CornerRadius="0,0,50,50" Margin="0,-10,0,0">
                  <TextBlock Text="&#xE74B;" FontFamily="Segoe MDL2 Assets" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,10,0,0"/>
             </Border>
        </Grid>
    </Grid>
</UserControl>
