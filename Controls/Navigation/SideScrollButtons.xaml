<UserControl x:Class="ccc.Controls.Navigation.SideScrollButtons"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="100">
    <StackPanel Orientation="Horizontal">
        <!-- Up Chevron -->
        <Button Width="24" Height="24" Style="{StaticResource InvisibleBtn}" Margin="0,0,2,0"
                Command="{Binding ScrollUpCommand}">
            <TextBlock Text="▲" FontSize="10" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </Button>
        
        <!-- Center Dot (Scroll to Active) -->
        <Button Width="24" Height="24" Style="{StaticResource InvisibleBtn}"
                Command="{Binding ScrollToActiveCommand}">
            <Grid Width="6" Height="6" Background="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" >
                <Border CornerRadius="3" Background="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
            </Grid>
        </Button>

        <!-- Down Chevron -->
        <Button Width="24" Height="24" Style="{StaticResource InvisibleBtn}"
                Command="{Binding ScrollDownCommand}">
            <TextBlock Text="▼" FontSize="10" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </Button>
    </StackPanel>
</UserControl>
