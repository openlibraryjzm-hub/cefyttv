<UserControl x:Class="ccc.Controls.Visuals.AudioVisualizer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <!-- The visualizer canvas -->
        <Canvas x:Name="VisCanvas" HorizontalAlignment="Center" VerticalAlignment="Center" Width="300" Height="300" RenderTransformOrigin="0.5,0.5">
             <Canvas.Effect>
                <!-- Optional Glow -->
                <DropShadowEffect Color="White" BlurRadius="4" ShadowDepth="0" Opacity="0.4"/>
            </Canvas.Effect>
        </Canvas>
    </Grid>
</UserControl>
