<UserControl x:Class="ccc.Views.BrowserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cef="clr-namespace:CefSharp.Wpf;assembly=CefSharp.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Browser Toolbar -->
        <Border Grid.Row="0" Background="#f0f0f0" Padding="5">
            <StackPanel Orientation="Horizontal">
                <Button Content=" + New Tab " Click="NewTab_Click" Padding="10,2"/>
                <TextBlock Text="CefSharp Browser" VerticalAlignment="Center" Margin="10,0" Foreground="#555"/>
            </StackPanel>
        </Border>

        <!-- Tabs -->
        <TabControl x:Name="BrowserTabs" Grid.Row="1" BorderThickness="0">
            <TabItem Header="Google">
                <cef:ChromiumWebBrowser Address="https://www.google.com"/>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
